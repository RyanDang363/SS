"""Indexing / registration utilities for RAGGERS.

Submodules are imported explicitly by callers, e.g.
``from video_rag.index.register_video import register_video``.
We deliberately do NOT re-export here, so ``python -m
video_rag.index.register_video`` doesn't trigger a double-import warning.
"""
